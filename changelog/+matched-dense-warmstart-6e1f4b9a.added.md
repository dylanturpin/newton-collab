Add opt-in identity-matched dense contact warm start to `SolverFeatherPGS` via `pgs_warmstart_matched` (with `pgs_warmstart_decay`, default 1.0): persistent contacts' dense normal/friction rows are seeded from their own previous-step impulses through the collision pipeline's `rigid_contact_match_index` (fresh contacts stay cold, slot-layout shifts cold-start the contact family), and the seeded impulses' velocity response is folded into the solve's starting velocity so nothing is applied twice. Requires the `Contacts` buffer to be built with `contact_matching` enabled; default off, leaving numerics unchanged.
