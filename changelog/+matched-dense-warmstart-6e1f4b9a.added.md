Add opt-in identity-matched dense contact warm start to `SolverFeatherPGS` via `pgs_warmstart_matched` (with `pgs_warmstart_decay`, default 1.0): persistent contacts' dense normal/friction rows are seeded from their own previous-step impulses through the collision pipeline's `rigid_contact_match_index`, instead of `pgs_warmstart`'s raw slot-index reuse — fresh contacts stay cold and slot-layout shifts cold-start the contact family, lifting the unreduced-contacts caveat on dense warm starting. Requires the `Contacts` buffer to be built with `contact_matching` enabled; default off, leaving numerics unchanged.
