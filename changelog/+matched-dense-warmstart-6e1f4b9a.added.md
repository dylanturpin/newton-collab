Make opt-in `SolverFeatherPGS.pgs_warmstart` identity-safe for contact and friction rows across dense, matrix-free, and propagation routing: persistent contacts seed from their own previous-step impulses through `rigid_contact_match_index`, variable friction spans validate ownership on both frames, carried impulses scale with `dt / prev_dt`, and fresh contacts stay cold. Non-contact dense rows also cold-start because their runtime allocation does not provide an identity contract. The mode requires contact matching, remains unsupported with body-pair contact reduction, and defaults off.
