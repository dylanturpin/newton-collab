Add construction-time `parallel_tree=True` to opt in to parallel kinematic-tree branches in FeatherPGS forward dynamics, backward force reduction and state publication, including velocity-limited articulations. Keep serial execution as the default (`parallel_tree=False`), without tree-plan or branch-wrench allocations. Select execution groups and chain segmentation from topology; measure full-step performance before enabling because gains depend on the workload and GPU. Preserve joint mathematics, solver budgets and serial fallback for unsupported models.
