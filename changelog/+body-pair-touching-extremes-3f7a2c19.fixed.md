Keep the touching footprint of a compound body under body-pair contact reduction: a second family of spatial-extreme slots is open only to contacts with canonical separation `<= 0` (plus, under hysteresis, a hovering incumbent without its preference), so speculative candidates hovering inside the contact gap (a tumbler's wall hulls around its base disc) no longer win every footprint slot and leave the body rocking on one load-bearing contact (`CollisionPipeline.ContactReductionConfig(body_pairs=True)`).
