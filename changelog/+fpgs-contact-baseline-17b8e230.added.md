Support FeatherPGS warm starting with body-pair contact reduction and `contact_matching="latest"`, preserving retained contact identities and owned geometry history through sorting, reduction and selected-world resets.

Size the default body-pair reduction table with a material-group budget as well as reachable pairs, retaining patch headroom when isolated environments are replicated.
