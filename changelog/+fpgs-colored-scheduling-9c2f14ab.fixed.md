Speed up `SolverFeatherPGS` `articulated_contact_response="propagation-colored"` on dense single-world scenes: contact units are now colored by first-fit greedy edge coloring instead of round-based ticket bidding, and prescribed (kinematic) bodies no longer count as coloring conflicts. A 235-body pile went from 255 colors with half the units in the serial overflow tail to 67 colors with none, and the sweep from 7.9 ms to 1.34 ms per iteration.
