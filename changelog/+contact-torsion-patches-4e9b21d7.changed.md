Allow experimental FeatherPGS contact torsion (`contact_torsion_radius`) together with persistent friction patches (`friction_anchor_beta > 0`): each patch region receives one spin row bounded by the region's pooled normal load times the undivided friction coefficient, less the tangent impulses of its anchor rows, and a positive radius with an omitted `friction_anchor_beta` now keeps the default patch friction instead of warning and selecting point friction. No migration is needed; pass `friction_anchor_beta=0` to keep the previous point-friction torsion behavior.
